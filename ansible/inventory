# Ansible Inventory - Zomato DevOps Pipeline
# Single instance setup: Jenkins + Application on same server
# Update the ansible_host IP after running terraform apply

[servers]
# Combined Jenkins + App server (single instance)
# IP will be updated after terraform apply
zomato-server ansible_host=PLACEHOLDER_IP ansible_user=ubuntu

[servers:vars]
# Python interpreter on Ubuntu
ansible_python_interpreter=/usr/bin/python3

# Application deployment directory
app_dir=/home/ubuntu/zomato-app

# Environment
environment=production

[local]
localhost ansible_connection=local
